# ============================================
# HAFJET Cloud Accounting - Backend Config Template
# TEMPLATE FILE - Copy to .env.production and fill in values
# ============================================

# INSTRUCTIONS:
# 1. Copy this file to .env.production
# 2. Generate secure secrets using: .\scripts\generate-secrets.ps1
# 3. Replace all placeholder values with actual values
# 4. NEVER commit .env.production to git!

# Server Configuration
NODE_ENV=production
PORT=3001
FRONTEND_URL=https://app.yourdomain.com

# Database Configuration (CRITICAL!)
# Replace with your MongoDB connection string
MONGO_URI=mongodb://admin:YOUR_MONGO_PASSWORD@mongo:27017/hafjet-bukku?authSource=admin

# JWT Configuration (CRITICAL!)
# Generate using: .\scripts\generate-secrets.ps1
JWT_SECRET=YOUR_JWT_SECRET_32_CHARS_MINIMUM
JWT_EXPIRE=7d

# Redis Configuration
# Replace with your Redis password
REDIS_URL=redis://:YOUR_REDIS_PASSWORD@redis:6379

# Malaysian Tax Settings
SST_RATE=0.06
GST_RATE=0.06

# LHDN E-Invoice Configuration (Optional - Configure when ready)
LHDN_API_BASE_URL=https://api.myinvois.hasil.gov.my
LHDN_API_KEY=your-lhdn-api-key-here
LHDN_CLIENT_ID=your-lhdn-client-id-here
LHDN_CLIENT_SECRET=your-lhdn-client-secret-here

# Email Configuration (Optional - Configure when ready)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-specific-password
EMAIL_FROM=noreply@hafjetaccounting.my

# Error Tracking (Optional - Configure when ready)
SENTRY_DSN=

# File Upload Configuration
MAX_FILE_SIZE=10485760
UPLOAD_DIR=./uploads

# Session Configuration (should match JWT_SECRET)
SESSION_SECRET=YOUR_JWT_SECRET_32_CHARS_MINIMUM

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# POS Integration (Optional)
LOYVERSE_SYNC_SCHEDULER_ENABLED=false
LOYVERSE_API_TOKEN=

# ============================================
# IMPORTANT CONFIGURATION STEPS:
# ============================================
# 1. Generate secure secrets: .\scripts\generate-secrets.ps1
# 2. Replace 'yourdomain.com' with your actual domain
# 3. Update MongoDB and Redis passwords
# 4. Configure LHDN API credentials for e-Invoice
# 5. Setup email service for notifications
# 6. Add Sentry DSN for error tracking
# 7. Validate configuration: .\scripts\validate-env.ps1
# ============================================

{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd /app/frontend && npm install && npm run build && cd /app/backend && npm install && npm run build && mkdir -p public && cp -r /app/frontend/dist/* public/"
  },
  "deploy": {
    "startCommand": "cd /app/backend && npm start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}

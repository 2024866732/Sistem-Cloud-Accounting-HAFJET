# ============================================
# HAFJET Cloud Accounting - Docker Compose Config Template
# TEMPLATE FILE - Copy to .env.production and fill in values
# ============================================

# INSTRUCTIONS:
# 1. Copy this file to .env.production
# 2. Generate secure secrets using: .\scripts\generate-secrets.ps1
# 3. Replace all placeholder values with actual values
# 4. NEVER commit .env.production to git!

# Application URLs (Replace with your actual domain)
VITE_API_URL=https://api.yourdomain.com/api
VITE_APP_URL=https://app.yourdomain.com

# Database Passwords (CRITICAL - Generate using scripts/generate-secrets.ps1)
MONGO_ROOT_PASSWORD=YOUR_MONGO_ROOT_PASSWORD_24_CHARS
REDIS_PASSWORD=YOUR_REDIS_PASSWORD_16_CHARS

# Monitoring
GRAFANA_ADMIN_PASSWORD=YOUR_GRAFANA_ADMIN_PASSWORD_16_CHARS

# Error Tracking (Optional)
SENTRY_DSN=
VITE_SENTRY_DSN=

# ============================================
# PRODUCTION DEPLOYMENT COMMAND:
# ============================================
# docker-compose -f deploy/docker-compose.prod.yml up -d
#
# IMPORTANT:
# 1. Generate secrets first: .\scripts\generate-secrets.ps1
# 2. Validate environment: .\scripts\validate-env.ps1
# 3. Test backup/restore: .\scripts\test-backup-restore.sh
# 4. Then deploy with command above
# ============================================
